version: "3"

services:
  nft_tracker:
    container_name: nft_tracker
    build: ./
    image: nft_tracker/latest
    env_file: ./.env
    environment:
      - DB_URL=$DB_URL
      - DB_DATABASE=$DB_DATABASE
      - DB_USER=$DB_USER
      - DB_PASSWORD=$DB_PASSWORD
    restart: unless-stopped
    ports:
      - 8080:8080
    environment:
      - NODE_ENV=development
